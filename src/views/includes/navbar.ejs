<% if (!saveUser || saveUser.theme == "padrao") { %>
  <nav class="navbar navbar-expand-lg bg-light">
<% } else { %>
  <nav class="navbar navbar-expand-lg bg-dark">
<% } %>
  <div class="container-fluid">
      <a class="navbar-brand" href="/" style="color:cyan">Agenda</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <% if (user) { %>
            <li class="nav-item">
              <a class="nav-link" href="/contato" style="color:cyan">Cadastrar Contato</a>
            </li>
          <% } %>
          <% if (!user) { %>
          <li class="nav-item">
            <a class="nav-link" href="/login" style="color:cyan">Entrar</a>
          </li>
          <% } %>
          <% if (user) { %>
            <li class="nav-item">
              <a class="nav-link" href="/logout" style="color:cyan">Sair</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/<%= user._id %>" style="color:cyan"> <%= user.username %> </a>
            </li>
          <% } %>
        </ul>
      </div>
  </div>
</nav>